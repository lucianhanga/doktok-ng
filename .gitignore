# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.uv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# Node / JS
node_modules/
dist/
build/
.vite/
*.tsbuildinfo
.pnpm-store/

# Env / secrets
.env
.env.*
!.env.example
!.env.staging.example
!.env.production.example

# DokTok runtime storage (never commit ingested documents).
# Per-tenant lifecycle folders live under storage/files/{tenant}/..., so ignore the whole tree.
storage/files/

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Build / supply-chain artifacts
sbom/*.json
*.log

# Agent scratch memory (not part of the project)
.claude/

# Local backup staging repository (M12; staged here before offsite sync)
backups/
