[Unit]
Description=Push the DokTok backup repo to Azure Blob hourly, incremental (offsite leg, GFS)

[Timer]
# Hourly: the incremental transport uploads only churn, so the 1h offsite RPO is cheap (#827).
OnCalendar=hourly
Persistent=true

[Install]
WantedBy=timers.target
