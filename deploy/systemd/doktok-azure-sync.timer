[Unit]
Description=Push the DokTok backup repo to Azure Blob daily (offsite leg, GFS)

[Timer]
# 03:47 daily: after the nightly window, before the workday; the offsite RPO is ~1 day by design.
OnCalendar=*-*-* 03:47:00
Persistent=true

[Install]
WantedBy=timers.target
